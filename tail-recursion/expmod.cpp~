#include <iostream>

int expmod(int b, int e, int n)
{
    int acc = 1;
EXPMOD:
    if (e == 0) return acc;
    else
    {
    }
}

int main()
{
    for (int i = 0; i < 10; i++)
    {
        std::cout << pow(2, i) % 1000
                  << ' '
                  << expmod(2, i, 1000) << std::endl;
    }
}
